<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.GithubRepositories" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:background">@color/light_gray</item>
        <item name="colorPrimary">@color/light_gray</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Base.Theme.GithubRepositories.ActionBar.Transparent" parent="Theme.Material3.DayNight">
        <item name="android:windowContentOverlay">@null</item>
        <item name="windowActionBarOverlay">false</item>
        <item name="colorPrimary">@android:color/transparent</item>
        <item name="android:background">@color/light_gray</item>
    </style>

    <style name="Base.Theme.GithubRepositories.RecyclerView" parent="Theme.Material3.DayNight">
        <item name="android:background">@color/white</item>
        <item name="colorPrimary">@color/light_gray</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:radius">20dp</item>
    </style>

    <style name="Theme.GithubRepositories" parent="Base.Theme.GithubRepositories" />
</resources>